description: This step executes the 'blackdagger version' gives version
tags: blackdagger, pull, configuration
steps:
  - name: get-CART-YAMLS
    command: bash
    script: |
      blackdagger version