description: This step executes the 'blackdagger pull default' command using bash to retrieve the default configuration or set of YAML files from the 'blackdagger' tool or repository.
tags: blackdagger, pull, default
steps:
  - name: get-DEFAULT-YAMLS
    command: bash
    script: |
      blackdagger pull default
