description: This step executes the 'blackdagger pull cart' using bash to retrieve the latest CART settings or data essential for the system's or application's CART functionality.

tags: blackdagger, pull, cart
steps:
  - name: get-CART-YAMLS
    command: bash
    script: |
      blackdagger pull cart