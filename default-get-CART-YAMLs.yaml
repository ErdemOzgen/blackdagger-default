tags: blackdagger, pull, cart
steps:
  - name: get-CART-YAMLS
    description: |
      Retrieves the cart-related YAML configurations or files using the 'blackdagger pull cart' command. This action fetches the latest cart settings or data essential for the system's or application's cart functionality.
    command: bash
    script: |
      blackdagger pull cart
